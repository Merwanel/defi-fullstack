{
  "watch": ["backend"],
  "ignore": ["backend/vendor", "backend/coverage", "backend/node_modules"],
  "ext": "php,json,yml,yaml,sh",
  "exec": "docker compose down -v && docker compose up --build",
  "delay": 1000,
  "verbose": true
}
